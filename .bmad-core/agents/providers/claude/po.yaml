# Claude-specific configuration for po agent
provider: claude
model: sonnet # Use Sonnet model for product ownership tasks
tools: all # Inherit all available tools
color: teal # Color for visual distinction

# Native agent wrapper settings
wrapper:
  name: BMAD-po
